import 'package:flutter/material.dart';

class FormVenda extends StatelessWidget {
  const FormVenda({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Teste'),
    );
  }
}